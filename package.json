{
  "devDependencies": {
    "@types/node": "^26.1.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test test/*.spec.mjs",
    "test:coverage": "node --test --experimental-test-coverage test/*.spec.mjs"
  }
}
